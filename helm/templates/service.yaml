apiVersion: v1
kind: Service
metadata:
  name: {{ include "genealogy.fullname" . }}
  labels:
    {{- include "genealogy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "genealogy.selectorLabels" . | nindent 4 }}

